const chai = require('chai');
const expect = chai.expect();
const chaiHttp = require('chai-http')

chai.use(chaiHttp);

describe('The social network API', () =>{
	it('should return return a list of events for GET /events', (done) => {
		
	})

})